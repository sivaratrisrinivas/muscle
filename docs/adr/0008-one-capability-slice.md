# Ship one capability, not a second product flow

The spine is discover, save, replay, and escalate. Get savings balance covers that. A second open-sub-account capability would add a form, a confirmation checkpoint, and a validation injector without a new must-have. We cut it. The mock may still show an Open sub-account button so the risky rule is real.
