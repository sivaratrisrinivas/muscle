# Capabilities version as new files, not an approval state machine

Reviewers need to see what changed. `get_savings_balance.v1.json` is the unit. Bump the integer when steps, locators, params, outputs, or the checkpoint change. Do not edit v1 in place. Draft/approved gating is a stretch we are not building.
