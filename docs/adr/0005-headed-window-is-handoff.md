# The headed browser is the operator surface

The brief wants a real control transfer and allows a stub console. Opening a second browser would be a different session. Pause leaves the Playwright window up, writes `intervention.json`, and waits for Enter. We record owner flips. No resume CLI, no co-browse UI, no compiling human clicks into the capability.
