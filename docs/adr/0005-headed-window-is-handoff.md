# The headed browser is the operator surface

The brief wants a real control transfer and allows a stub console. Opening a second browser would be a different session. Pause leaves the Playwright window up, writes `intervention.json`, and waits for Enter or `bun run resume`. We record owner flips. We do not build a co-browse UI, and we do not compile the human's clicks into the capability.
