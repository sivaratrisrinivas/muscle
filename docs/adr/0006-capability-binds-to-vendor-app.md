# A capability binds to a vendor app and version, not a tenant

Hundreds of institutions run the same core. Re-recording per tenant would not scale. The artifact names a vendor app and a version. A tenant overlay may change origin, a few labels, and optional locator overrides. Checkpoint failure is drift: escalate. Do not silently rewrite the capability. We will not implement overlays in this slice.
